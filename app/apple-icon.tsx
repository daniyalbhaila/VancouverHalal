import { IconGenerator } from '@/lib/icon-generator';

export const size = {
    width: 180,
    height: 180,
};

export const contentType = 'image/png';

export default function AppleIcon() {
    return IconGenerator(180);
}
